[color]
	ui = true
[alias]
	root = !pwd
	ci = commit
	co = checkout
	cl = clone
	st = status -s
	s = status
	lol = "log --graph --decorate --pretty=oneline --abbrev-commit --all"
	db = "checkout -b"
[include]
	path = ".gitconfig.local"
[push]
	default = current
[gh]
	autoUpdate = always
